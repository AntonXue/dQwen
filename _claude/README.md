# `_claude/` — dQwen working log & progression

**Project goal.** A **release-quality evaluation package** for the dQwen3.5 / dQwen3
family and its diffusion-LM comparators (LLaDA, Dream / Dream-Coder, SDAR), all under
**one environment**, with lm-eval-harness on top. Two things it must support:

1. Load any model and call **its own shipped sampler** — "the number with LLaDA's own
   sampler".
2. Run **our sampler** on the same weights — "the number with ours". Model x sampler
   is a cross-product, not one number per model.

Plus reproducing the numbers those papers claim, so our comparison tables are
defensible rather than cited.

**Why this dir.** The finer-grained running timeline — what was run, the numbers, the
decisions and their rationale, the negatives — at low ceremony, so a fresh Claude (or
future-me) on a different machine can reconstruct the *progression* without git
archaeology or chat scrollback. Mirrors `~/foo/ADLMC/_claude/`.

## Conventions
- **Filename:** `yyyymmdd-hhmmss-short-description.md`. One topic per file.
- **Append-only:** don't rewrite old entries; add a new file that supersedes and
  reference the old one by name. The directory IS the timeline.
- **Write for the next reader:** 1–2 lines of context, then findings. Tables > prose
  for numbers. Always say *what was run*, *where artifacts live* (paths / HF repo ids),
  and *what to do next*.
- **Record refuted priors explicitly.** Several load-bearing design choices here exist
  because a measurement contradicted an earlier belief; without the refutation written
  down, the next reader re-proposes the wrong thing.
- Cold-start reading order: this dir in filename order → `third_party/LOCKFILE.md`
  (which upstream implementation produced which published table) → the auto-memory
  index at `~/.claude/projects/-home-ayx98-foo-ADLMC/memory/MEMORY.md`.

## Sister projects
- `~/foo/ADLMC/` — the research repo this is extracted from. Training, ablations, and
  the eval prehistory (`ablations/paper_evals/`, `eval/`). Its `_claude/` holds the
  dQwen3.5 family training history and the eval-standardization decisions.

## Index (update when adding files)

| file | what it holds |
|---|---|
| `20260814-011801-DELEGATION-coda-gh200-silent-failure-investigation.md` | ⭐ **CLUSTER DELEGATION: CoDA forward = noise on GH200** (chance mmlu, 0.00 gen; build gates passed so tied-head/non-finite-rope are RULED OUT). Carries workstation goldens (inv_freq 5.150445, lm_head sum/std, logit stats, argmax lists, probe script) so diagnosis is diffing not guessing; prime suspect = finite-but-WRONG inv_freq from the post-load repair under torch 2.10/aarch64. Rerun protocol: delete 19 cells (idempotency would skip!), --pending, relaunch, re-sync. |
| `20260814-041500-HANDOFF-slurm-claude-campaign-final-pickup-list.md` | ⭐⭐ **HANDOFF (cluster -> workstation): campaign FINAL — 1,154/1,154, zero INCOMPLETE, 19 CoDA cells refreshed post-fix; merge snapshot at _runs/20260814-040500-campaign-merge/. Exact rsync/verify pickup steps; restamped CoDA row (HE 23.17 = PROBE exact; ⚠ mmlu 26.21 below expected band — check for a PROBE-era prior, else disclose). Hardened rope gate now runs on EVERY family load incl. workstation (a trip there is a REAL find). Ranked open threads (CoDA-768 cell now meaningful; 4B fence anomaly; decontamination). Cluster housekeeping notes.** |
| `20260814-023800-coda-rootcause-zero-invfreq-fixed.md` | ⭐⭐ **CoDA root cause: torch 2.10/aarch64 materialises meta buffers as ZEROS — finite, so the repair's isfinite trigger never fired — all-zero inv_freq = position-blind model.** The canvas-filling \slowness\ was the failure signature, not protocol. Fixes: recompute UNCONDITIONALLY (coda.py) + assert_finite_rope hardened with the inv_freq[0]==1.0 invariant (all families). Post-fix: goldens EXACT digit-for-digit incl. logit stats; gens coherent; 9 families pass hardened gate. 19 cells deleted, mop-up staged (2 jobs). Sidebar: the stall-night was the idev node's Lustre client wedging; batch nodes clean. |
| `20260814-011325-campaign-landed-validation-verdict.md` | ⭐⭐ **Campaign landed: 1,154/1,154 cells in _runs/grid_v1_vista/ (separate store; grid_v1 stays the PROBE record). Validation PASSES: mmlu exact, gen benchmarks ±2pp noise, gsm8k moves = the predicted AR-canvas fix (2B +5.38). control@50k row exists at last. ⚠ CoDA = garbage on GH200 (chance mmlu, 0.00 gen — the silent-load class; load_matrix never ran there): fix + rerun 19 cells before its row restamps.** summarize grew --store. |
| `20260721-184634-dqeval-bootstrap-port-evidence-adapters-samplers.md` | ⭐⭐ **Repo bootstrap + the port evidence.** Our models need tf 5.13, comparators pin 4.4x–4.5x, and the sets are **disjoint** — so LLaDA/Dream/SDAR were **ported** with ~90 lines of shims (3/4/3, zero invented). **Decomposed the drift: 100% torch, 0% transformers ⇒ the port is BITWISE-exact and the acceptance gate is bitwise, not a tolerance** (an earlier tolerance recommendation came from a confounded comparison — do not reintroduce). **Silent failure class** hit 2/3 families: tf5 meta-device wipes non-persistent rotary `inv_freq`, tf5's repair lives in base `_init_weights` which Dream and SDAR both override ⇒ model loads, forwards, returns noise, raises nothing (argmax 20–40%) — hence mandatory goldens + `assert_finite_rope`. **Two logit surfaces** (`raw_logits`/`logits`): Dream alone is AR-aligned and shifts in its own sampler AND eval wrapper, so canonicalising for its native sampler would double-shift silently. **Two priors refuted:** Dream's sampler is NOT HF-coupled (plain class, 6/7 bit-identical), and SDAR DOES ship a sampler (`JetAstra/SDAR/generate.py`, runnable on HF weights via `store_kv`). **SDAR minefield:** four disagreeing implementations, thresholds 0.85/0.75/**0.9**, published table came from **LMDeploy**, and their own script can't do greedy ⇒ our SDAR numbers are re-measurements. **Built + GPU-verified:** all four families load through one adapter contract (incl. dQwen3.5 at `revision=step30000-swa`), and the unified bs=1 sampler runs on all four — **token-for-token identical to LLaDA's own `generate.py`** at matched config, which validates "native decode is a preset, not a code path". |
| `20260805-190500-v1-v3-mixture-ab-mbpp-format-effect-and-footgun-guards.md` | ⭐⭐ **0.8B v1-vs-v3 mixture A/B (budget-matched, both 50k) + the MBPP prompt-format effect.** v3 wins every benchmark and **code ROSE ~4.4pp while code share FELL 71%→50%** — code was bought with targeting (`code_sft` 12%, verified concepts), not volume; the campaign's assumed "sacrifice code for general gains" trade may not exist. v1 arm **reproduces July on all four benchmarks (two bitwise)**, which is what makes the deltas quotable. **MBPP's `[BEGIN]`/`[DONE]` scaffold is 0.000% of both training corpora** ⇒ no scaffold-leakage story is available, and swapping it for a ```python fence is worth **+4.00pp (v1) / +6.00pp (v3)** — v1-fenced ≈ v3-scaffolded, i.e. **prompt format ≈ the entire mixture overhaul**. Mechanism is the *task→fenced-solution pattern* (0%→17% of tokens), NOT fences (v1 already had 42.9%). ⚠ fenced MBPP is NOT comparable to published LLaDA/Dream numbers. **Priors refuted:** cross-process nondeterminism does NOT perturb these evals (pinning `CUBLAS_WORKSPACE_CONFIG` changed nothing — the real cause was `--num-fewshot` silently falling back to a 0-shot task default); no Hub drift; v1 does not ramble on fences. **Harness hardened:** append now needs `--allow-append` (fails pre-load), few-shot warning per task, `decode`+`model_args` provenance in every result json, config banner at log head, and a **`.gitignore` bug** that left the whole agent worktree untracked-but-visible. |
| `20260807-035500-coda-port-five-shims-and-same-backbone-head-to-head.md` | ⭐⭐ **CoDA ported (5th family) + the first SAME-BACKBONE head-to-head.** `Salesforce/CoDA-v0-Base` is adapted from Qwen3-1.7B — same backbone as our `dQwen3-1.7B-Base` — so it is the only comparator isolating RECIPE rather than starting weights. Unified protocol: **ours 42.68 HE vs CoDA 23.17 (+19.51)**, and that is with our superseded v1-era mixture. ⚠ their published 29.3 uses 768 tokens at ONE token/step (768 forwards) vs our ≤512 with up to 32 committed/step — our protocol undersells them ~6pp, so run the native-ish cell before publishing. ⚠ CoDA **untied its head** (verified from the raw checkpoint: max|Δ| 4.41, cos 0.48) ⇒ **2.032B**, not 1.7B. Their arXiv table is mislabelled (README says "Humaneval Instruct" for base rows) and their MBPP+ > MBPP because "MBPP+" is EvalPlus's 378-problem *sanitized* subset, NOT a superset ⇒ **their MBPP is not comparable to ours; HumanEval is the only clean target**. **Five shims, all restorations, three NEW beyond `ADLMC/coda/old/environment.md`** (which was verified on 4.57; they break only on 5.x): `_supports_sdpa`, `rope_scaling→None`, **`tie_word_embeddings→False` (silent head-destruction if unrestored)**, `all_tied_weights_keys={}`, post-load `inv_freq` recompute. CoDA is the SECOND AR-aligned family (same verbatim shift as Dream) — docstring corrected. Lesson: a 3× slowdown I blamed on decode was **GPU contention** — check `nvidia-smi` first. |
| `20260814-011500-campaign-complete-1154-cells.md` | ⭐⭐ **CAMPAIGN COMPLETE: 1,154/1,154 GATE cells, ~21h wall, 14 jobs, ONE transient failure, ONE wall-clip — both mopped up.** Cost model ~3% off except CoDA: not slow per-forward (0.035s), it CANVAS-FILLS (n_forward=1024 on every doc vs 61 for 2B; no stops under base prompting, matches their fixed-768 protocol). Raw = _runs/grid_v1, 381M, $HOME. rsync handoff recipe inside (archive PROBE grid_v1 first). Final merge readout -> next entry. |
| `20260813-071500-part1-ar-baselines-landed-validation-green.md` | ⭐ **Part 1 (AR twins) LANDED — tiled SLURM path validation GREEN.** 72/72 cells, 3h09m (est 3.2h), zero failures. Full GATE AR table inside. Diff vs workstation: MMLU exact (±0.2), GSM8K noise-level except **bare 2B +5.4pp = the _ARLM canvas fix landing on the documented verbosity pathological**; math500 re-measurements sit above full-MATH refs as expected. Parked: 4B mbpp-fence −16pp curiosity. |
| `20260813-034500-qos-caps-tiled-launch.md` | **QOS caps (MaxJobsPU=20/MaxSubmitPU=40, array tasks COUNT; sbatch login-only) kill the array interface — campaign launches TILED**: plan_tiles.py packs cells into ~3.2h model-contiguous node-lanes from measured GH200 rates, 16 lanes/job, 8h wall; launch.sh submits. 13 jobs total (1/6/6), est 542 node-h, worst lane 3.3h. sbatch_cells.sh demoted to single-cell debugging. |
| `20260813-023723-math500-mbpp-accel-ar-canvas-rulings.md` | ⭐⭐ **Pre-greenlight rulings executed: MATH500 replaces MATH-5000** (anchor already spent by the math_verify ruling; verified vs LLaDA/Dream primary sources first — both run full minerva_math), **MBPP joins the acceleration grid, ~250-doc shards, @25k symmetry rows, AR fence cells, NO v1 arm.** ⚠ REVIEW FIND FIXED: HFLM hardcodes max_gen_toks=256 — the 1024-canvas ruling was silently skipping the AR side (fatal on MATH tails); _ARLM subclass wires BENCH gen in. Campaign regenerated: 1,154 cells (72/272/810), ~420-450 GPU-h. Gates + smokes green. |
| `20260813-005207-cluster-bringup-campaign-manifests.md` | ⭐ **First cluster session: bring-up green (harness layer installed, both gates PASS on Vista) + the campaign authored as `manifests/` — part1 AR baselines 56 / part2 big table 611 / part3 acceleration 837 cells, composition from the paper repo's EVAL-REQUEST/FINDING/HANDOFF docs + six Anton rulings (HE+GSM8K in part 3, full-canvas = stretch tier, ports deferred, part 1 = validation launch, ≤200-doc shard rule [mmlu exempt: shared-forward, ~1 fwd/doc], full MATH not MATH500).** Flow: manifests pushed for review; NO sbatch until greenlight. Compute nodes have network (prewarm ran from idev). ⚠ antlr 4.11 vs omegaconf in the shared training env. |
| `20260812-185410-prewarm-verb-and-git-hygiene.md` | **--prewarm became run.py's third manifest verb** (prewarm_caches.py deleted; lifecycle = --prewarm -> --pending -> sbatch -> summarize --merge; live-tested on the 16-cell manifest). **Git hygiene:** three fully-merged July branches + the stale .claude/worktrees/eval agent worktree (the `+` in git branch) removed after verifying 0-ahead + clean; `git branch` = `* main` only. |
| `20260812-160000-slurm-deploy-kit-and-16-cell-battery.md` | ⭐⭐ **SLURM kit (TACC Vista GH200/aarch64, OFFLINE compute) + 16-cell battery all green through the literal manifest+index interface.** Kit: sbatch_cells.sh, prewarm_caches.py, run.py --pending, cluster-safe setup_env.sh (never touches hand-built torch), summarize --merge (disjoint-union coalesce). ⚠ TWO BUGS: LLaDA's remote __init__ re-enables flash sdp (clobbers determinism pin; run.py now re-pins AFTER model build — caught BY the provenance field); doc_id leaked into metric means. Merge validated: 4-stripe gsm8k union == whole run ±0.23pp (HFLM bs=16 batching; POLICY: run AR cells unsharded). |
| `20260812-124500-benchmarks-as-python-files-yaml-layer-deleted.md` | ⭐⭐ **Benchmarks are Python files (Anton: skimmability over DRY): one config file per benchmark in dqeval/tasks/, yaml layer + include_path + setup PATCH 3 (site-packages grading patch) all deleted; lm-eval = engine only.** Frozen MECHANICALLY from the pin (fewshot literals + 57-subject MMLU table extracted, minerva logic imported-not-copied); permanent gate tests/task_freeze_gate.py = fingerprints + byte-identical renders, PASSED over all 70 leaves. ⚠ TRAP: lm-eval's dict loader pops 'task' and unregistered tasks run as [Task: None] → grid._restore_names() repairs. Task names & store unchanged; all 8 benchmarks smoked via run.py. **Same-day reshape: grid+harness -> cell_runner.py (single entry, run_cell(Cell(...))); adapter->models, sampler->samplers (owns GenOutput; models/samplers AST-checked orthogonal), families->model_families, tasks->benchmarks; root run.py = the only login-node file. Then: hidden importlib dispatch inverted (load+BUILDERS live with the families) and everything model-shaped closed into dqeval/models/ (adapter.py + samplers.py + 5 families + catalog/load in __init__) — families import siblings only; then dqeval/ DISSOLVED entirely: models/ + benchmarks/ at repo root, cell_runner merged into run.py, packaging deleted, structure gate asserts the generic names resolve to this repo. Then benchmarks/ collapsed into benchmark_specs.py (one section per benchmark; data doesn't need file isolation) — final root: run.py + summarize.py + benchmark_specs.py + models/ + tests/. Pruning pass: SDAR family + flash_attn/einops deps, 404/instruct registry rows (24->15), dream native wrapper (YAGNI; ships in their repo), upstream clone-hatch — llada.generate_native KEPT as the engine's oracle (single parity gate, re-verified).** |
| `20260812-113000-lmeval-only-plus-columns-evalplus-removed.md` | ⭐⭐ **lm-eval-only ruling: '+' columns are grid cells; EvalPlus deleted.** Grader A/B (10 models x 164 saved HE+ gradings): 9/1640 verdict flips, family cells EXACT, worst AR delta 1.22pp (HumanEval/92 systematic). HE/HE+ prompts byte-identical across frameworks; MBPP+ prompts are NOT (EvalPlus docstring-0shot vs our scaffold-3shot). ⚠ stock lm-eval `mbpp_plus` NEVER RUNS THE PLUS SUITE (targets test_list[0..2]) — local `mbpp_plus_full` task fixes it. **Chasing the flips found a real bug: the Table-4 HEval+ fill read plus_status alone; OFFICIAL '+' = base∧plus — four AR cells corrected (-0.61 to -1.22pp), official-semantics agreement 4/1640** (2 timeouts + 2 upstream suite-skew cases). regrades/ frozen as the EvalPlus-graded record; MBPP+ column = re-measurement (tripwire vs published numbers). |
| `20260812-094500-runs-layout-contract-raw-vs-launches.md` | ⭐ **Layout contract (Anton's rulings): `_runs` = raw only; every launch dir stamped `yyyymmdd-hhmmss-<desc>` (retroactive stamps from dir birth times); coalescing = read-only `summarize.py`; NO second top-level tree** (a brief `_launches/` split was reverted — repair happens IN `_runs`). grid_v1 cell filenames stay deterministic ON PURPOSE (identity=filename ⇒ idempotent reruns + multi-node shards); launch time = `launched_at` in the meta record. Full old→new rename map inside (battery, battery4b, coda, ar_twin, stepsweep, smoke_tau, ar_overnight, heplus→regrades). ar_overnight held only logs — data was always grid_v1. Auditor + budget grid re-verified after the moves. |
| `20260812-023000-grid-deploy-gates-passed.md` | ⭐⭐ **grid_v1 runner DEPLOY-READY** (run.py + README.md are the front door): spot-check 10/10 (6 decode families × 6 benchmarks × ours/comparator/AR × 3 revisions × 6 stripe shapes); Gate A shard merge LOSSLESS (164/164, 0 overlap) w/ HE reproduction +1.83pp attributed to cross-era numerics (same-env kernel probe: 0 flips ⇒ deterministic; expect ~±2pp on PROBE→GATE restamps, paired same-env comparisons exact); Gate B GSM8K template fidelity 163/165 per-doc, stripe aggregate identical. τ grid {0.5–0.9}; smoke frontiers: block Pareto-dominates standard (~4× forwards at ≤ accuracy). Merge-tool notes: 2 sample records/doc on gsm8k (strict = per-doc min); global id = k + doc_id·n. NEXT: make_manifest row lists, then launch. |
| `20260814-025900-summarize-heval-plus-silent-blank-column.md` | ⭐ **summarize.py silently blanked the WHOLE HumanEval+ column (22 cells)** — `headline()` did an exact task lookup, `HEAD_TASK['humaneval-plus']` = `humaneval_plus`, but cells carry lm-eval's `humaneval_plus_sound` → `?`. Data was always fine; the aggregation layer could not read it, and it fails toward "missing run" rather than "wrong number". FIX: exact lookup, then a UNIQUE prefix match, else keep `?` (so real problems still surface). Before/after over the vista store: 22 `?` → 0, all 22 changed lines are `? ?`→value, and NO previously-readable cell changed. Also records the harvest gotchas: MMLU on DLMs is decode `mc-nelbo` not the block decode; `humaneval` reports `pass@1` while `mbpp` reports `pass_at_1`; and math500's `exact_match` vs `math_verify` disagree in OPPOSITE directions for AR vs DLM rows, so the 08-12 AR-column ruling should not be applied blindly to DLM rows. |
| `20260814-121922-PLAN-gsm8k-4shot-rerun-and-the-metric-audit-behind-it.md` | ⭐⭐⭐ **PICKUP DOC for the cluster: GSM8K moves 8-shot → 4-shot, 632 cells re-run**, plus the audit that produced the decision. AUDIT: MATH500's two metrics are broken in OPPOSITE directions — `math_verify` fails closed on unparseable answers (`$Navin$`, `$\textbf{(B)}$`, unbalanced braces) costing DLM rows ~2-5pp; strict `exact_match` fails on Minerva-template deviation, costing post-trained AR models 31% of the set (AR Qwen3.5-4B/9B both land on exactly 20.40, below the 2B's 34.80). strict/union coverage: 95-98% on ALL DLM rows vs 37.6%/39.4% on those two AR cells. The metric choice FLIPPED §4.1's MATH500 architecture claim (control +0.037 / hybrid +0.011 / tie), so it could not be published either way. GSM8K by contrast is clean — DLM strict == flexible EXACTLY — and `flexible-extract` is NOT a robustness check (AR 2B: 38.51 vs 58.98 strict). REFERENCES: LLaDA uses lm-eval ONLY for likelihood tasks and an unreleased INTERNAL library for GSM8K/MATH/HumanEval/MBPP (quoted), decoding at steps=length=1024 — so only our MMLU reproduction is apples-to-apples, which §4's credibility paragraph currently overstates; Qwen3.5 publishes NOTHING on our benchmarks (no tech report; card carries MMLU-Pro/GPQA-D/HMMT under chat+temp1.0+80k tokens); Qwen3's report DOES publish base numbers under a near-matching protocol (Qwen3-1.7B-Base MMLU 62.63 / GSM8K 75.44 / MATH 43.50 / MBPP 55.40; 0.6B-Base 52.81 / 59.59 / 32.44 / 36.60) — and SDAR's "75.4" is that table's 75.44. Three-way protocol table shows we match on MMLU-5 and HumanEval-0, no consensus on MBPP (keep 3), and are the sole outlier on GSM8K. PLAN: Phase 0 freeze (num_fewshot=4, version bump, ARCHIVE the 8-shot cells as a free protocol-sensitivity result); **Phase 1 = validation gate, ~10 cells, RUN FIRST** — new `Qwen3-{0.6,1.7}B-Base` anchors checked against 59.59/75.44, >3pp = FAIL and STOP; Phase 2 = 84 headline cells (unblocks the manuscript); Phase 3 = 540 acceleration cells (not blocking). Shard by cost (12 for 4B/9B, keep 6 for 0.8B/2B). ⚠ The `-Base` models are CALIBRATION ANCHORS, never AR counterparts — counterparts stay the bare post-trained repos. Also proposes (undecided) a grading contract: stock metric in the table + one robust metric per cell + an stderr-based publication gate. |
| `20260814-123751-gsm8k-4shot-phase0-frozen.md` | ⭐ **Phase 0 of the 4-shot rerun EXECUTED** (workstation): BENCH shots 8→4 (authoritative) + spec/gate sync + task version 4.0; summarize.py MIXED-PROTOCOL merge guard; 632 8-shot cells archived to `_runs/grid_v1_vista_gsm8k_8shot/`; freeze gate PASSED at 4-shot; cluster inherits Phases 1–3 (archive its gsm8k/ first — idempotency would fast-skip otherwise). |
| `20260814-125102-protocol-alignment-reverified-rerun-manifests.md` | ⭐⭐ **Alignment re-verified from primary sources; grid GO** — full shots table vs Qwen3 §3.3 / LLaDA repo / Dream script; NEW: LLaDA lm-eval repro pins OUR lm_eval==0.4.8 (same grader path) + used stock 3-shot MBPP (keep-3 strengthened) + Dream is the 8-shot house (archive = Dream convention). Rerun manifests cut: phase1 16 (anchors+AR, gate vs Table 8), phase2 84 (manuscript blocker), phase3 540. |
| `20260814-130859-HANDOFF-gsm8k-4shot-rerun-for-the-slurm-claude.md` | ⭐⭐⭐ **THE cluster execution brief for the 4-shot rerun** (self-sufficient): STEP-ZERO archive-before-run trap (idempotency fast-skips otherwise), prewarm (2 new -Base anchors), Phase 1 gate table vs Qwen3 Table 8 (>3pp = STOP, anchors are NEVER counterparts), Phase 2 = manuscript blocker (ship early), Phase 3 trailing, tiling/re-shard latitude, done-checklist (104 merged groups / 634 in gsm8k/ / shots==4 spot-check / zero MIXED-PROTOCOL), standing cautions. |
| `20260814-133042-REVERSAL-gsm8k-stays-8shot-no-rerun.md` | ⭐⭐⭐ **REVERSAL (Anton): GSM8K stays stock 8-shot; the 4-shot rerun is CANCELLED** — SLURM Claude must STAND DOWN on the 130859 handoff (no cluster work exists). Full revert verified: shots=8, freeze gate PASSED, rerun manifests deleted, 632 cells back in the store (1,154 whole, 264/0/0 merge). SURVIVES: MATH500 metric rulings, credibility-paragraph fix, merge guard, alignment table (now the disclosure). Manuscript harvest fully unblocked. |
| `20260816-000500-RULING-gsm8k-8shot-forever-part5d-dropped.md` | **RULING (Anton): GSM8K is 8-SHOT, PERIOD — gsm8k-4shot dropped entirely; part5d (176 cells) deleted.** Third and final swing of the 4-shot question (planned 08-14 → cancelled; revived 08-15 as twins; dropped 08-16) — DO NOT RE-PROPOSE. part5 = 873 static + 418 τ = 1,291 cells; BENCH["gsm8k-4shot"] stays inert. |
| `20260815-122500-part4-complete-455-cells-results.md` | ⭐⭐ **Part 4 COMPLETE: 455/455 decode-axis cells, ZERO failures, zero wall-clips — store = 1,609, merge 364 groups zero INCOMPLETE (counts exact: +55 HE/+300 gsm8k/+100 mbpp). Gate beat the queue; no special τ handling. Both §4 figures now drawable. Handback rsync inside; merge snapshot at _runs/20260815-122000-part4-merge/. Calibration note: flat τ=0.6 factor optimistic on comparator gsm8k (~1.4×).** |
| `20260815-034500-part4a-tau-gate-all-four-pass.md` | **Part4a τ-gate: ALL FOUR comparators PASS** (verdict table inside; no special handling — the unified commit rule ran verbatim). τ0.9 = 2.3-2.7x fewer forwards at zero-to-negligible accuracy cost; LLaDA and Dream-Coder pass@1 IDENTICAL to their s32 anchors. Gate beat the queue; no cell ran ungated. 4b-4f licensed. |
| `20260815-024044-HANDOFF-decode-axis-part4-overnight-for-the-slurm-claude.md` | ⭐⭐⭐ **Tonight's cluster brief: part4 decode-axis runs** (manuscript EVAL-REQUEST 20260815-023130 made executable, self-sufficient for Vista): 4a τ-gate FIRST (4 cells, inlined pass criteria + s32 reference table incl. mean n_forward), 4b cheap sweeps (179), **4c = the payload** (207, the frontier figure cannot exist without it), 4d MBPP (60), 4f comparator/control ceilings (5; LLaDA's = its published decode). **455 cells total** — a 4e τ-tail (0.98/0.99, 252 cells) was staged then CUT by Anton ("not that ambitious"); the τ grid ends at 0.95 and the fast-end saturation is narrated as a finding. Additive-only, no archiving step, no new models, done-checklist + rsync-back + manuscript notes (τ0.95 already everywhere; HE+ accel = free CPU regrade) inlined. |
| `20260815-211016-EVAL-REQUEST-full-canvas-sweep-and-gsm8k-4shot.md` | ⭐⭐⭐ **THE FULL-CANVAS REQUEST (Anton, compute explicitly unconstrained): every comparator publishes FULL-CANVAS numbers and we measured them all in block32.** Receipts: LLaDA's appendix sets base decoding to steps=length=1024 with NO blocks (block-32 is their INSTRUCT setting); Dream and Dream-Coder's generation_utils.py contain ZERO occurrences of 'block' — their sampler cannot do it. Our lone full-canvas cell reproduces LLaDA HE 33.54 vs published 33.5 (cross-grader, see doc). Expect full-canvas to BECOME the main-table protocol and block32 to move to the appendix. Campaign A = standard-static-s{32..1024} x 14 rows x 4 benchmarks (924 cells, ~8x block cost/cell); Campaign B = GSM8K 4-shot ADDITIVELY alongside 8-shot (~1,476), via a distinct benchmark id so the MIXED-PROTOCOL guard never trips — this is NOT the cancelled 20260814 replacement rerun. Gates first: does standard-static work below N=1024, does standard-tau exist at all, is the s1024 forward count measured or nominal. ⚠ §1 is not a new finding — 20260814-121922 recorded it and it went unactioned. |
| `20260815-214527-HANDOFF-part5-fullcanvas-campaign-sequencing-for-the-slurm-claude.md` | ⭐⭐⭐ **part5 execution brief: the full-canvas campaign under Anton's compromise** (supersedes the 211016 request's scope tables; its why + gates stand): MATH500 carries the decode axes (consensus 4-shot; our 9B LEADS comparators there vs trailing all on gsm8k), GSM8K = big-table cells only (new additive `gsm8k-4shot` id + one full-canvas s1024 config per shot count), AR twins unsharded (request error fixed). Six manifests 6/405/84/176/280/418 (static 951, +418 gated τ); gates incl. does-sN-respect-N + does-standard-tau-exist; 4c pre-answered from source (stop check is block-granular → static n_forward exactly N). Wave plan for multi-day qgh tiling; static cells price by pure arithmetic (docs×N, zero variance). |
