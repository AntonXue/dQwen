# dqeval target environment -- ONE env runs every family.
#
# This is only possible because LLaDA / Dream / SDAR are PORTED to transformers 5.x
# via the shims in dqeval/families/*/compat.py. Their native pins are 4.4x-4.5x and
# none of them loads unmodified here. See docs/ for the port evidence.
#
# torch is pinned exactly. The port shims were verified to be BITWISE exact against
# transformers 4.57 *at fixed torch*; changing torch changes numbers (measured
# max|d| 4.69e-01 between torch 2.5.1+cu121 and 2.7.1+cu128 on identical weights),
# so torch is part of the reproducibility contract, not an incidental dependency.

torch==2.7.1                    # +cu128 build
transformers==5.13.0            # Qwen3.5 family requires >= 5.x
accelerate==1.14.0
flash-linear-attention==0.5.1   # GatedDeltaNet kernels (dQwen3.5 hybrid)
tilelang==0.1.12                # required on Hopper with triton >= 3.4
flash_attn==2.8.3               # SDAR imports this unguarded at module top level
einops==0.8.1                   # SDAR
# evalplus was removed 2026-08-12: the '+' columns run as ordinary grid
# cells (humaneval_plus stock task; mbpp_plus_full local task -- stock
# mbpp_plus never executes the plus suite). Grader A/B vs EvalPlus on
# 1640 saved HE+ gradings: 9 verdict flips, family cells exact.
datasets==4.4.1

# Phase 3 (lm-eval harness) -- not required for adapters/samplers:
#   lm_eval==0.4.8
# NOTE lm_eval 0.4.8 needs two source patches under transformers 5.x
# (hf_vlms.py -> AutoModelForImageTextToText). Document as an install step.
